/**
 * Prisma Configuration File (préparatoire pour Prisma 7+)
 * 
 * Ce fichier sera activé lors de la migration vers Prisma 7 (prévue février 2025).
 * Actuellement, le projet utilise Prisma 6.19.0 et ce fichier est commenté pour éviter les erreurs de build.
 * 
 * Instructions pour la migration vers Prisma 7 :
 * 1. Décommenter le code ci-dessous
 * 2. Supprimer la propriété `url` du datasource dans schema.prisma
 * 3. Mettre à jour @prisma/client vers la version 7.x
 * 
 * @see https://pris.ly/d/config-datasource
 * @see https://pris.ly/d/prisma7-client-config
 */

// import { defineConfig } from '@prisma/client';

// export default defineConfig({
//   datasources: {
//     db: {
//       url: process.env.DATABASE_URL,
//     },
//   },
// });
